{
  "name": "apexlegends",
  "version": "1.0.0",
  "description": "Apex Legends Stats Tracker App",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "server": "nodemon server",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "sageOMEGA",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  }
}
