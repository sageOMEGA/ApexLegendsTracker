import React, { Fragment } from "react";
import HomeContainer from "../layouts/HomeContainer";

const Home = () => {
  return (
    <Fragment>
      <HomeContainer />
    </Fragment>
  );
};

export default Home;
